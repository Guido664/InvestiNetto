// Questo servizio è stato disabilitato poiché le funzionalità AI sono state rimosse.
import { InvestmentParams, CalculationResult } from "../types";

export const getFinancialInsight = async (
  params: InvestmentParams, 
  results: CalculationResult
): Promise<string> => {
  return "Analisi AI non disponibile.";
};